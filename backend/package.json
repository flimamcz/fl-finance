{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "db": "dotenv -e .env -- npx sequelize db:drop && dotenv -e .env -- npx sequelize db:create && dotenv -e .env -- npx sequelize db:migrate && dotenv -e .env -- npx sequelize db:seed:all",
    "db:drop": "env $(cat .env) npx sequelize db:drop"
  },
  "keywords": [],
  "author": "Filipe Lima",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^8.6.0",
    "dotenv-cli": "^11.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.6.0",
    "nodemon": "^3.0.1",
    "sequelize": "^6.3.4",
    "sequelize-cli": "^6.2.0"
  }
}
